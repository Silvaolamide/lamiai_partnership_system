@extends('layouts.admin')
@section('content')
<div class="space-y-6"><div><h1 class="text-2xl font-bold">Payment Disputes</h1><p class="mt-1 text-slate-500">Customer payment issues requiring platform support.</p></div>@if(session('success'))<div class="rounded-xl bg-emerald-50 p-4 text-emerald-700">{{ session('success') }}</div>@endif<div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-4">Order</th><th class="px-5 py-4">Customer</th><th class="px-5 py-4">Reason</th><th class="px-5 py-4">Submitted</th><th class="px-5 py-4">Status</th><th></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($disputes as $dispute)<tr><td class="px-5 py-4 font-mono font-semibold">{{ $dispute->order->order_number }}</td><td class="px-5 py-4">{{ $dispute->customer?->name ?: $dispute->order->customer_name }}</td><td class="px-5 py-4"><div class="font-semibold">{{ $dispute->reason }}</div><div class="mt-1 max-w-md text-xs text-slate-500">{{ Str::limit($dispute->message,120) }}</div></td><td class="px-5 py-4">{{ $dispute->created_at->diffForHumans() }}</td><td class="px-5 py-4"><span class="rounded-full px-3 py-1 text-xs font-bold {{ $dispute->status==='resolved'?'bg-emerald-100 text-emerald-700':'bg-amber-100 text-amber-700' }}">{{ ucfirst($dispute->status) }}</span></td><td class="px-5 py-4 text-right">@if($dispute->status==='open')<form method="POST" action="{{ route('admin.payment-disputes.resolve',$dispute) }}">@csrf @method('PATCH')<button class="font-semibold text-emerald-600">Resolve</button></form>@else<span class="text-slate-400">Resolved</span>@endif</td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-slate-500">No payment disputes.</td></tr>@endforelse</tbody></table></div><div class="p-5">{{ $disputes->links() }}</div></div></div>
@endsection
