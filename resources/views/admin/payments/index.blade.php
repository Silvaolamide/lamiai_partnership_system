@extends('layouts.admin')
@section('content')
<div class="space-y-6"><div><h1 class="text-2xl font-bold">Payment Verification</h1><p class="mt-1 text-slate-500">Review bank-transfer proofs before orders become paid.</p></div><div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-4">Order</th><th class="px-5 py-4">Buyer</th><th class="px-5 py-4">Amount</th><th class="px-5 py-4">Submitted</th><th class="px-5 py-4">Status</th><th></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($payments as $payment)<tr><td class="px-5 py-4 font-mono font-semibold">{{ $payment->order->order_number }}</td><td class="px-5 py-4">{{ $payment->order->customer_name ?: $payment->order->customer?->name }}<div class="text-xs text-slate-500">{{ $payment->order->customer_email }}</div></td><td class="px-5 py-4 font-bold">₦{{ number_format($payment->amount,2) }}</td><td class="px-5 py-4">{{ $payment->created_at->diffForHumans() }}</td><td class="px-5 py-4"><span class="rounded-full px-3 py-1 text-xs font-bold {{ $payment->status==='confirmed'?'bg-emerald-100 text-emerald-700':($payment->status==='rejected'?'bg-red-100 text-red-700':'bg-amber-100 text-amber-700') }}">{{ ucfirst($payment->status) }}</span></td><td class="px-5 py-4 text-right"><a href="{{ route('admin.payments.show',$payment) }}" class="font-semibold text-blue-600">Review →</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-slate-500">No payment submissions yet.</td></tr>@endforelse</tbody></table></div><div class="p-5">{{ $payments->links() }}</div></div></div>
@endsection
