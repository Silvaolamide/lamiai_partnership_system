@extends('layouts.admin')
@section('content')
<div class="mx-auto max-w-5xl space-y-6"><div><a href="{{ route('admin.payments.index') }}" class="text-sm font-semibold text-blue-600">← Payment verification</a><h1 class="mt-2 text-2xl font-bold">Review {{ $payment->order->order_number }}</h1></div><div class="grid gap-6 lg:grid-cols-3"><div class="space-y-6 lg:col-span-2"><section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="text-lg font-bold">Payment details</h2><dl class="mt-5 grid gap-5 sm:grid-cols-2"><div><dt class="text-xs uppercase text-slate-500">Amount</dt><dd class="mt-1 text-2xl font-bold">₦{{ number_format($payment->amount,2) }}</dd></div><div><dt class="text-xs uppercase text-slate-500">Bank</dt><dd class="mt-1 font-semibold">{{ $payment->bank_name }}</dd></div><div><dt class="text-xs uppercase text-slate-500">Reference</dt><dd class="mt-1 font-mono font-semibold">{{ $payment->transaction_reference }}</dd></div><div><dt class="text-xs uppercase text-slate-500">Transfer date</dt><dd class="mt-1 font-semibold">{{ optional($payment->transfer_date)->format('d M Y') }}</dd></div></dl></section><section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="text-lg font-bold">Proof of payment</h2><a href="{{ Storage::url($payment->proof_path) }}" target="_blank" class="mt-4 block"><img src="{{ Storage::url($payment->proof_path) }}" alt="Payment proof" class="max-h-[600px] w-full rounded-xl object-contain bg-slate-100 p-3"></a><p class="mt-2 text-xs text-slate-500">Click the proof to open the original file.</p></section></div><aside class="space-y-6"><section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-bold">Expected platform account</h2><p class="mt-4 font-semibold">{{ $paymentSettings->bank_name }}</p><p>{{ $paymentSettings->account_name }}</p><p class="mt-1 font-mono text-lg">{{ $paymentSettings->account_number }}</p></section>@if($payment->status==='pending')<section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><form method="POST" action="{{ route('admin.payments.confirm',$payment) }}">@csrf @method('PATCH')<button class="w-full rounded-xl bg-emerald-600 px-4 py-3 font-bold text-white hover:bg-emerald-700">✓ Confirm Payment</button></form><form method="POST" action="{{ route('admin.payments.reject',$payment) }}" class="mt-4 space-y-3">@csrf @method('PATCH')<textarea name="rejection_reason" required placeholder="Reason for rejecting this payment..." class="w-full rounded-xl border-slate-200"></textarea><button class="w-full rounded-xl bg-red-50 px-4 py-3 font-bold text-red-700 hover:bg-red-100">Reject Payment</button></form></section>@else<section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p class="font-bold">{{ ucfirst($payment->status) }}</p>@if($payment->rejection_reason)<p class="mt-2 text-sm text-red-700">{{ $payment->rejection_reason }}</p>@endif</section>@endif</aside></div></div>
@endsection
