<x-app-layout>
<x-slot name="header"><div><h2 class="font-black text-xl text-gray-900">{{ $campaign->name }} · All Leads</h2><p class="text-sm text-gray-500 mt-1">{{ $campaign->owner?->name ?? $campaign->owner?->email ?? 'Business' }} · {{ $leads->total() }} leads</p></div></x-slot>
<div class="py-8"><div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"><div class="mb-5 flex flex-wrap items-center justify-between gap-3"><a href="{{ route('admin.marketing-campaigns.index') }}" class="rounded-xl border border-gray-200 bg-white px-4 py-2 text-sm font-bold text-gray-700 hover:bg-gray-50">← All campaigns</a><a href="{{ route('admin.marketing-campaigns.download', $campaign) }}" class="rounded-xl bg-teal-700 px-4 py-2 text-sm font-black text-white hover:bg-teal-800">Download all leads (CSV)</a></div><div class="overflow-hidden rounded-2xl border border-gray-100 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-gray-50 text-xs uppercase tracking-wide text-gray-500"><tr><th class="px-6 py-3">Lead</th><th class="px-6 py-3">WhatsApp</th><th class="px-6 py-3">Campaign answers</th><th class="px-6 py-3">Attribution</th><th class="px-6 py-3">Date</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($leads as $lead)<tr class="align-top hover:bg-teal-50/30"><td class="px-6 py-4"><div class="font-bold">{{ $lead->name }}</div><div class="text-xs text-gray-500">{{ $lead->email }}</div></td><td class="px-6 py-4 whitespace-nowrap">{{ $lead->whatsapp_number }}</td><td class="px-6 py-4 min-w-[320px]"><div class="space-y-3">@forelse($campaign->configuredQuestions() as $question)<div><div class="text-[11px] font-black uppercase tracking-wide text-gray-400">{{ $question['label'] }}</div><div class="font-medium text-gray-800">{{ data_get($lead->responses, $question['id']) ?: '—' }}</div></div>@empty<span class="text-gray-400">No custom questions.</span>@endforelse</div></td><td class="px-6 py-4"><div class="font-medium">{{ $lead->utm_source ?: 'Direct' }}</div><div class="text-xs text-gray-500">{{ $lead->utm_campaign ?: '' }}</div></td><td class="px-6 py-4 whitespace-nowrap text-gray-500">{{ $lead->created_at->format('d M Y, H:i') }}</td></tr>@empty<tr><td colspan="5" class="px-6 py-12 text-center text-gray-500">No leads yet.</td></tr>@endforelse</tbody></table></div>@if($leads->hasPages())<div class="border-t px-6 py-4">{{ $leads->links() }}</div>@endif</div></div></div>
</x-app-layout>
