<x-app-layout>
<x-slot name="header"><div><p class="text-xs font-black uppercase tracking-widest text-violet-600">Money allocation</p><h2 class="font-black text-2xl text-gray-900">Commissions</h2><p class="text-sm text-gray-500 mt-1">See who earned what, why, against which order, and what still needs action.</p></div></x-slot>
<div class="py-6 bg-slate-50 min-h-screen"><div class="max-w-[1600px] mx-auto px-4 sm:px-6 lg:px-8">@include('admin.partials.command-nav')
@if(session('success'))<div class="mb-4 rounded-2xl bg-emerald-50 border border-emerald-200 p-4 text-emerald-700">{{session('success')}}</div>@endif @if(session('error'))<div class="mb-4 rounded-2xl bg-rose-50 border border-rose-200 p-4 text-rose-700">{{session('error')}}</div>@endif
<div class="grid grid-cols-2 lg:grid-cols-4 gap-3 mb-5"><div class="rounded-2xl bg-white border p-5"><span class="text-xs text-slate-400">Available</span><b class="block text-xl mt-2">₦{{number_format($stats['total_available'],2)}}</b><small class="text-slate-400">{{$stats['count_available']}} records</small></div><div class="rounded-2xl bg-white border p-5"><span class="text-xs text-slate-400">Payable</span><b class="block text-xl mt-2 text-violet-700">₦{{number_format($stats['total_payable'],2)}}</b><small class="text-slate-400">{{$stats['count_payable']}} records</small></div><div class="rounded-2xl bg-white border p-5"><span class="text-xs text-slate-400">Paid</span><b class="block text-xl mt-2 text-emerald-700">₦{{number_format($stats['total_paid'],2)}}</b><small class="text-slate-400">Historical payouts</small></div><div class="rounded-2xl bg-white border p-5"><span class="text-xs text-slate-400">Reversed</span><b class="block text-xl mt-2 text-rose-700">₦{{number_format($stats['total_reversed'],2)}}</b><small class="text-slate-400">Excluded from earnings</small></div></div>
<div class="rounded-2xl border bg-white shadow-sm p-5 mb-5"><form method="GET" action="{{route('admin.commissions.index')}}" class="grid grid-cols-1 md:grid-cols-5 gap-3"><select name="status" class="rounded-xl border-slate-200"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{$status}}" @selected(request('status')==$status)>{{ucfirst($status)}}</option>@endforeach</select><select name="level" class="rounded-xl border-slate-200"><option value="">All levels</option>@for($i=1;$i<=4;$i++)<option value="{{$i}}" @selected(request('level')==$i)>Level {{$i}}</option>@endfor</select><select name="program_id" class="rounded-xl border-slate-200"><option value="">All programs</option>@foreach($programs as $program)<option value="{{$program->id}}" @selected(request('program_id')==$program->id)>{{$program->name}}</option>@endforeach</select><input type="number" step="0.01" name="min_amount" value="{{request('min_amount')}}" placeholder="Minimum commission" class="rounded-xl border-slate-200"><div class="flex gap-2"><button class="flex-1 rounded-xl bg-slate-900 text-white font-black">Apply</button><a href="{{route('admin.commissions.index')}}" class="rounded-xl border px-4 py-2.5 font-bold">Reset</a></div></form></div>
<div class="rounded-2xl border bg-white shadow-sm overflow-hidden"><div class="p-5 border-b"><h3 class="font-black">Commission ledger</h3><p class="text-sm text-slate-500">Each row is an allocation; open it to inspect the rule and money trail.</p></div><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-slate-500"><tr><th class="p-4 text-left">Recipient</th><th class="p-4 text-left">Order</th><th class="p-4">Level</th><th class="p-4 text-left">Program</th><th class="p-4 text-right">Base → commission</th><th class="p-4">Status</th><th></th></tr></thead><tbody class="divide-y">@forelse($commissions as $commission)<tr class="hover:bg-violet-50/30"><td class="p-4"><div class="font-black">{{$commission->partner->user->name}}</div><div class="text-xs text-slate-400">{{$commission->partner->partner_code}}</div></td><td class="p-4"><a class="font-mono font-bold text-violet-600" href="{{route('admin.orders.show',$commission->order_id)}}">{{$commission->order->order_number}}</a></td><td class="p-4 text-center"><span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-black">L{{$commission->level}}</span></td><td class="p-4">{{$commission->order->program->name}}</td><td class="p-4 text-right"><div class="font-black">₦{{number_format($commission->commission_amount,2)}}</div><div class="text-xs text-slate-400">{{$commission->rate}}% of ₦{{number_format($commission->base_amount,2)}}</div></td><td class="p-4"><span class="rounded-full px-2.5 py-1 text-xs font-black {{in_array($commission->status,['paid'])?'bg-emerald-100 text-emerald-700':($commission->status==='payable'?'bg-violet-100 text-violet-700':($commission->status==='reversed'?'bg-rose-100 text-rose-700':'bg-amber-100 text-amber-700'))}}">{{ucfirst($commission->status)}}</span></td><td class="p-4"><a class="font-black text-violet-600" href="{{route('admin.commissions.show',$commission)}}">Details →</a></td></tr>@empty<tr><td colspan="7" class="p-10 text-center text-slate-400">No commissions found.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{$commissions->withQueryString()->links()}}</div></div>
</div></div></x-app-layout>
