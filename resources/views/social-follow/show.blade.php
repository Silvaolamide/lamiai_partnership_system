<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $campaign->name }} · {{ $business->name }}</title><script src="https://cdn.tailwindcss.com"></script></head><body class="min-h-screen bg-slate-950 text-white"><main class="mx-auto max-w-3xl px-4 py-8 sm:py-14"><div class="text-center"><p class="text-sm font-semibold uppercase tracking-widest text-indigo-300">{{ $business->name }}</p><h1 class="mt-3 text-3xl font-black sm:text-5xl">{{ $campaign->headline ?: $campaign->name }}</h1><p class="mx-auto mt-4 max-w-2xl text-slate-300">{{ $campaign->description }}</p></div>
@if(!$participant)<div class="mt-10 rounded-3xl bg-white p-6 text-slate-900 shadow-2xl sm:p-8"><h2 class="text-xl font-bold">Follow us to unlock your reward</h2><p class="mt-2 text-sm text-slate-500">We do not need your social-media handle. Just visit each profile, follow or subscribe, return here, and confirm.</p><form method="POST" action="{{ route('social-follow.start', [$business->slug, $campaign->slug]) }}" class="mt-6">@csrf<button class="w-full rounded-2xl bg-indigo-600 px-5 py-4 font-bold text-white hover:bg-indigo-500">Start →</button></form></div>
@else @php($verified=$participant->verifications->whereIn('status',['verified','claimed'])->pluck('social_account_id')->all()) @php($score=$campaign->socialAccounts->whereIn('id',$verified)->sum(fn($a)=>(int)$a->pivot->points))
<div class="mt-10 rounded-3xl bg-white p-6 text-slate-900 shadow-2xl sm:p-8"><div class="flex items-end justify-between"><div><p class="text-sm font-medium text-slate-500">YOUR SOCIAL SCORE</p><p class="mt-1 text-5xl font-black text-indigo-600">{{ $score }}<span class="text-2xl text-slate-400"> / {{ $campaign->socialAccounts->sum(fn($a)=>(int)$a->pivot->points) }}</span></p></div><div class="text-right"><p class="text-xs uppercase text-slate-400">Required</p><p class="font-bold">{{ $campaign->minimum_score }}</p></div></div><div class="mt-5 h-3 overflow-hidden rounded-full bg-slate-100"><div class="h-full rounded-full bg-indigo-600 transition-all" style="width:{{ min(100, ($score/max(1,$campaign->minimum_score))*100) }}%"></div></div><div class="mt-6 space-y-3">@foreach($campaign->socialAccounts as $account) @php($done=in_array($account->id,$verified))<div class="flex items-center justify-between rounded-2xl border p-4"><div><p class="font-bold capitalize">{{ $account->platform }}</p><p class="text-xs text-slate-500">{{ $done ? 'Follow confirmed on this device' : 'Follow / subscribe, then confirm' }}</p></div>@if($done)<span class="rounded-full bg-green-100 px-3 py-1 text-xs font-bold text-green-700">✓ Done</span>@else<div class="flex gap-2"><a target="_blank" rel="noopener" href="{{ $account->profile_url }}" class="rounded-xl bg-slate-900 px-3 py-2 text-xs font-bold text-white">Follow ↗</a><form method="POST" action="{{ route('social-follow.verify', [$business->slug,$campaign->slug,$account]) }}">@csrf<button class="rounded-xl border px-3 py-2 text-xs font-bold">I did it</button></form></div>@endif</div>@endforeach</div>
@if($score >= $campaign->minimum_score)<div class="mt-7 rounded-2xl bg-green-50 p-5 text-center"><p class="text-lg font-black text-green-800">🎉 You're unlocked!</p><a href="{{ route('social-follow.unlock', [$business->slug,$campaign->slug]) }}" class="mt-4 inline-block rounded-xl bg-green-600 px-6 py-3 font-bold text-white">{{ $campaign->resource_title ?: 'Get your resource' }}</a></div>@else<div class="mt-7 rounded-2xl bg-slate-50 p-5 text-center"><p class="font-bold text-slate-800">Almost there!</p><p class="mt-1 text-sm text-slate-500">Complete {{ $campaign->minimum_score - $score }} more point(s) to unlock your reward.</p></div>@endif</div>@endif
<p class="mt-8 text-center text-xs text-slate-500">{{ $business->name }} · Social Follow Gate</p></main></body></html>
