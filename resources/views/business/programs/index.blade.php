@extends('business.portal')
@section('title','Affiliate Programs') @section('heading','Affiliate Programs')
@section('content')
<div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 mb-6"><div><p class="text-slate-500">Create, monitor and control the programs that power your partner sales.</p></div><a href="{{route('business.onboarding',['step'=>'commission'])}}" class="brand rounded-xl px-5 py-3 text-sm font-black text-white">+ Create program</a></div>
<div class="grid gap-5 md:grid-cols-2 xl:grid-cols-3">@forelse($programs as $program)<article class="bg-white border rounded-2xl p-6 shadow-soft"><div class="flex justify-between gap-3"><div><h2 class="font-black text-lg">{{$program->name}}</h2><p class="text-xs text-slate-400 mt-1">{{$program->slug}}</p></div><span class="rounded-full px-3 py-1 text-xs font-black {{ $program->status==='active'?'bg-emerald-50 text-emerald-700':($program->status==='paused'?'bg-amber-50 text-amber-700':'bg-slate-100 text-slate-600')}}">{{strtoupper($program->status)}}</span></div><p class="text-sm text-slate-500 mt-4 line-clamp-2">{{$program->description ?: 'No program description yet.'}}</p><div class="grid grid-cols-3 gap-2 mt-6"><div class="bg-slate-50 rounded-xl p-3"><b class="block">{{$program->partners_count}}</b><span class="text-[11px] text-slate-400">Affiliates</span></div><div class="bg-slate-50 rounded-xl p-3"><b class="block">{{$program->orders_count}}</b><span class="text-[11px] text-slate-400">Orders</span></div><div class="bg-slate-50 rounded-xl p-3"><b class="block">{{$program->products_count}}</b><span class="text-[11px] text-slate-400">Products</span></div></div><div class="mt-5 flex gap-2"><a href="{{route('business.programs.edit',$program)}}" class="flex-1 text-center rounded-xl bg-slate-950 text-white px-4 py-2.5 text-sm font-black">Manage</a><form method="POST" action="{{route('business.programs.toggle',$program)}}" class="flex-1">@csrf @method('PATCH')<button class="w-full rounded-xl border px-4 py-2.5 text-sm font-black">{{$program->status==='active'?'Pause':'Activate'}}</button></form></div></article>@empty<div class="md:col-span-3 bg-white border rounded-2xl p-12 text-center"><p class="text-xl font-black">No affiliate programs yet</p><p class="text-slate-500 mt-2">Launch your first program and recruit partners to sell your products.</p></div>@endforelse</div><div class="mt-6">{{$programs->links()}}</div>
@endsection