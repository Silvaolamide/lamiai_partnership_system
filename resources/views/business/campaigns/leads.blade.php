<x-app-layout>
<x-slot name="header"><div><h2 class="font-black text-xl text-gray-900">{{ $campaign->name }} · Leads</h2><p class="text-sm text-gray-500 mt-1">Review the people who completed this campaign.</p></div></x-slot>
<div class="py-8"><div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"><div class="overflow-hidden rounded-2xl border border-gray-100 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-gray-50 text-xs uppercase tracking-wide text-gray-500"><tr><th class="px-6 py-3">Lead</th><th class="px-6 py-3">WhatsApp</th><th class="px-6 py-3">Sold online</th><th class="px-6 py-3">What sold</th><th class="px-6 py-3">Sales</th><th class="px-6 py-3">Campaign answers</th><th class="px-6 py-3">Source</th><th class="px-6 py-3">Date</th></tr></thead><tbody class="divide-y divide-gray-100">@forelse($leads as $lead)<tr class="align-top hover:bg-teal-50/40"><td class="px-6 py-4"><div class="font-bold">{{ $lead->name }}</div><div class="text-xs text-gray-500">{{ $lead->email }}</div></td><td class="px-6 py-4 whitespace-nowrap">{{ $lead->whatsapp_number }}</td><td class="px-6 py-4">{{ $lead->has_sold_online ? 'Yes' : 'No' }}</td><td class="px-6 py-4 max-w-xs">{{ $lead->what_sold ?: '—' }}</td><td class="px-6 py-4">{{ $lead->sales_result ? ucwords(str_replace('_',' ', $lead->sales_result)) : '—' }}</td><td class="px-6 py-4 min-w-[260px]"><div class="space-y-2">@foreach($campaign->configuredQuestions() as $question)<div><div class="text-[11px] font-black uppercase tracking-wide text-gray-400">{{ $question['label'] }}</div><div class="font-medium text-gray-800">{{ data_get($lead->responses, $question['id']) ?: '—' }}</div></div>@endforeach</div></td><td class="px-6 py-4">{{ $lead->utm_source ?: 'Direct' }}{{ $lead->utm_campaign ? ' · '.$lead->utm_campaign : '' }}</td><td class="px-6 py-4 whitespace-nowrap text-gray-500">{{ $lead->created_at->format('d M Y, H:i') }}</td></tr>@empty<tr><td colspan="8" class="px-6 py-12 text-center text-gray-500">No leads yet.</td></tr>@endforelse</tbody></table></div>@if($leads->hasPages())<div class="border-t px-6 py-4">{{ $leads->links() }}</div>@endif</div></div></div>
</x-app-layout>
