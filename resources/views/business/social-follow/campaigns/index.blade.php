<x-app-layout>
<x-slot name="header"><div class="flex flex-wrap items-center justify-between gap-4"><div><h2 class="font-black text-xl text-gray-900">Social Follow Campaigns</h2><p class="text-sm text-gray-500 mt-1">Turn your social accounts into a gated reward experience.</p></div><div class="flex gap-2"><a href="{{route('business.social-follow.accounts')}}" class="rounded-xl border px-4 py-3 text-sm font-black">Social accounts</a><a href="{{route('business.social-follow.campaigns.create')}}" class="rounded-xl bg-teal-700 px-5 py-3 text-sm font-black text-white">+ Create campaign</a></div></div></x-slot>
<div class="py-8"><div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
@if(session('success'))<div class="mb-5 rounded-2xl bg-emerald-50 p-4 font-bold text-emerald-800">{{session('success')}}</div>@endif
<div class="grid gap-5 md:grid-cols-2 xl:grid-cols-3">@forelse($campaigns as $campaign)<div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex items-start justify-between gap-3"><div><span class="rounded-full px-3 py-1 text-xs font-black {{ $campaign->is_active?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-500' }}">{{ $campaign->is_active?'ACTIVE':'PAUSED' }}</span><h3 class="mt-4 text-lg font-black">{{$campaign->name}}</h3></div><span class="rounded-full bg-teal-50 px-3 py-1 text-xs font-black text-teal-700">{{$campaign->participants_count}} visitors</span></div><p class="mt-3 text-sm text-slate-500 line-clamp-2">{{$campaign->headline}}</p><div class="mt-4 rounded-2xl bg-slate-50 p-4"><p class="text-xs font-black uppercase tracking-wider text-slate-400">Required score</p><p class="mt-1 text-xl font-black">{{$campaign->minimum_score}}</p></div><div class="mt-5 flex flex-wrap gap-2"><a target="_blank" href="{{route('social-follow.show',$campaign->slug)}}" class="rounded-xl bg-slate-900 px-3 py-2 text-sm font-black text-white">Preview</a><a href="{{route('business.social-follow.campaigns.edit',$campaign)}}" class="rounded-xl border px-3 py-2 text-sm font-bold">Edit</a><form method="POST" action="{{route('business.social-follow.campaigns.toggle',$campaign)}}">@csrf @method('PATCH')<button class="rounded-xl border border-teal-200 px-3 py-2 text-sm font-bold text-teal-700">{{$campaign->is_active?'Pause':'Activate'}}</button></form></div><div class="mt-4 break-all text-xs font-bold text-teal-700">{{route('social-follow.show',$campaign->slug)}}</div></div>@empty<div class="md:col-span-2 xl:col-span-3 rounded-3xl border-2 border-dashed border-teal-200 bg-teal-50/50 p-14 text-center"><h3 class="text-xl font-black">No social campaigns yet</h3><p class="mx-auto mt-2 max-w-lg text-sm text-slate-500">Configure your social accounts first, then create a campaign that unlocks a resource when customers reach your required score.</p><a href="{{route('business.social-follow.campaigns.create')}}" class="mt-6 inline-block rounded-xl bg-teal-700 px-5 py-3 font-black text-white">Create your first campaign</a></div>@endforelse</div>@if($campaigns->hasPages())<div class="mt-6">{{$campaigns->links()}}</div>@endif</div></div>
</x-app-layout>
